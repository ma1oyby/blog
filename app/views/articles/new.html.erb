<h1>New Article</h1>

<%= form_for :article, url: articles_path do |a| %>
	<p>
		<%= a.label :title %><br>
		<%= a.text_field :title %>
	</p>
	<p>
		<%= a.label :text %>
		<%= a.text_area :text %>
	</p>

	<p>
		<%= a.submit %>
	</p>
<% end %>

<%= link_to 'Back to list', articles_path %>